import React from 'react'

const Logout = (props) => {
    return (
        <div>
            <button onClick={props.click}>
                logout
            </button>
        </div>
    )
}

export default Logout